import { TemplateHeaderDirective } from './template-header.directive';

describe('TemplateHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new TemplateHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
